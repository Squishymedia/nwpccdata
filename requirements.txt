# Django itself.
Django==1.6.1
# Deployment tool.
Fabric==1.3.2
# Excel import library.
openpyxl==1.8.3
# REST Framework dependencies.
djangorestframework==2.3.12
Markdown==2.3.1
django-filter==0.7
# Only needed if deploying with uWSGI.
uWSGI==2.0.1
# Only needed if using PostgreSQL.
psycopg2==2.4.5
# Used with FastCGI
flup==1.0.3.dev-20110405
